server:
  port: 8088

spring:
  application:
    name: configserver
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/MSreeN/configserver.git"
          search-paths: config
        clone-on-start: true
        default-label: main
        timeout: 5


